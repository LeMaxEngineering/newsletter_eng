{
  "name": "neewsletterengine",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter @neewsletter/web dev",
    "build:web": "pnpm --filter @neewsletter/web build",
    "dev:api": "pnpm --filter @neewsletter/api start:dev",
    "build:api": "pnpm --filter @neewsletter/api build",
    "lint": "pnpm lint:all",
    "lint:all": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
